// No data yet — add entries here in the same shape as other data files.
// (Left empty during the Base44 → static-data migration; pages handle the empty-state gracefully.)
export const testimonials = [];

export default testimonials;
